import 'package:flutter/material.dart';

class AppColors {
  // basic color && color app
  static const Color colorBackground = Color(0xffF6F6F6);
  static const Color colorBackgroundAppBar = Color(0xff03346E);
  static const Color colorBackgroundSheet = Color(0xffF0F0F0);
  static const Color colorBackgroundDrawer = colorBackground;

  // selected && unselected
  static const Color colorFixed = Colors.black;

  // color for text
  static const Color colorTitleTextWhite = Colors.white;
  static const Color colorTitleText2 = Color.fromRGBO(45, 35, 35, 1.0);
  static const Color colorTitleText3 = Colors.black38;

  // for icons
  static const Color colorIcons = Colors.white;
}

class AppColorsDark {
  // basic color && color app
  static const Color colorBackground = Color(0xff000000);
  static const Color colorBackgroundAppBar = Color(0xff03346E);
  static const Color colorBackgroundSheet = Color(0xff303030);
  static const Color colorBackgroundDrawer = Color(0xff334756);

  // color for text
  static const Color colorTitleTextWhite = Colors.white;
  static const Color colorTitleText2 = Color.fromRGBO(236, 232, 232, 1.0);
  static const Color colorTitleText3 = Colors.white70;

  // for icons
  static const Color colorIcons = Colors.white;
}
